<h1>Inventories</h1>
<%= link_to "New inventory", new_inventory_path, class: "btn new-color" %>
<%= render 'inventories_table' %>
<%= render 'layouts/pagination', page: @page, total_pages: @total_pages,
           per_page: @per_page, total_items: @total_items %>
